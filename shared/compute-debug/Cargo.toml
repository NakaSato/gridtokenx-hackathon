[package]
name = "compute-debug"
version = "0.1.0"
description = "Compute unit debugging macro for Anchor programs (localnet only)"
edition = "2021"

[lib]
name = "compute_debug"

[features]
default = []
localnet = []
idl-build = []

[dependencies]
anchor-lang = { workspace = true }
